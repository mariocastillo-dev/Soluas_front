export interface ComboBarLineChartData {
    mes: string;
    gasto: number;
    presupuesto: number;
}

export interface DoughnutChartData {
    labels: string[];
    values: number[];
}

